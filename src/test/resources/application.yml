spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lyrics
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true
        format_sql: true

  profiles:
    active: dev

jwt:
  secret: omgomgomgomgomgomgomgomgomgomgomgomgomgomgomgomg

auth:
  free-apis:
    /api/v1/auth/login

server:
  port: 8080
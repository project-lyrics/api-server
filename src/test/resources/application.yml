spring:
  datasource:
    url: jdbc:h2:tcp://localhost:9092/feelin
    username: feelin
    password: 1234
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true
        format_sql: true

  profiles:
    active: dev

jwt:
  secret: omgomgomgomgomgomgomgomgomgomgomgomgomgomgomgomg

auth:
  free-apis:
    /api/v1/auth/login

server:
  port: 8080